import numpy as np

identity_matrix_1 = np.identity(2)
print("Identity matrix using the identity func\n", identity_matrix_1)
identity_matrix_2 = np.eye(2)
print("Identity matrix using the eye func\n", identity_matrix_2)

# its practically the same except one does take longer which is the identity function
